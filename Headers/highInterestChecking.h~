#ifndef HIC_H
#define HIC_H
#include "noServiceChargeChecking.h"
class highInterestChecking: public noServiceChargeChecking
{
	const double higherInterest = 2.29, min_bal = 350;
	public:
  	highInterestChecking(string owner, string num, string dt, double bal);
	double calculateInterest(string today);
/*	double checkBalance(string today);
	void updateBalance(string today);*/
};
#endif
